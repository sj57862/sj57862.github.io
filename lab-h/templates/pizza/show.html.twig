    <a href="{{ path('app_pizza_edit', {'id': pizza.id}) }}">edit</a>

    {{ include('pizza/_delete_form.html.twig') }}
    </table>

    <div class="container py-3">
        <div class="card p-3 shadow-sm bg-white rounded-3">
            <h1>Pizza</h1>
                <td>{{ pizza.price }}</td>
            <div class="table-responsive rounded-3 overflow-hidden">
                <table class="table mb-0">
{% extends 'base.html.twig' %}

{% block title %}Pizza{% endblock %}

{% block body %}
    <h1>Pizza</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td><span class="badge bg-success rounded-pill">{{ pizza.price }}</span></td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pizza.name }}</td>
            </tr>
            <tr>
            </table>
        </div>
            <tr>
            <div class="mt-3">
                <a href="{{ path('app_pizza_index') }}" class="btn btn-secondary btn-sm">back to list</a>
                <a href="{{ path('app_pizza_edit', {'id': pizza.id}) }}" class="btn btn-outline-primary btn-sm ms-2">edit</a>
                {{ include('pizza/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
        </tbody>
    </table>

    <a href="{{ path('app_pizza_index') }}">back to list</a>

    <a href="{{ path('app_pizza_edit', {'id': pizza.id}) }}">edit</a>

    {{ include('pizza/_delete_form.html.twig') }}
{% endblock %}
