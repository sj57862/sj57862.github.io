<%- include('../partials/site-start', { title: typeof title !== 'undefined' ? title : 'New Pizza' }) %>

<div class="card p-3 rounded-3 bg-white">
  <div class="d-flex align-items-center mb-3">
    <h1 class="me-auto mb-0">Create new Pizza</h1>
    <a href="/pizza" class="btn btn-secondary btn-sm">back to list</a>
  </div>

  <%- include('_form', { pizza: pizza || {}, buttonLabel: 'Create' }) %>
</div>

<%- include('../partials/site-end') %>
